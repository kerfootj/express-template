{
    "configurations": [
        {
            "name": "Attach Debugger",
            "type": "node",
            "request": "attach",
            "remoteRoot": "/usr/app",
            "localRoot": "${workspaceFolder}"
        }
    ]
}
